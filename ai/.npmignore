# Exclude source and dev files from the packed package.
# dist/ is intentionally NOT listed here so it is included when
# npm packs this package (e.g. via --install-links from the ui layer).
src/
tests/
*.test.ts
tsconfig.json
.eslintrc.json
