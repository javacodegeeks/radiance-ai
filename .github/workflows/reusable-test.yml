name: Reusable Test

on:
  workflow_call:
    inputs:
      package-dir:
        description: Package directory to test (e.g. ai, data)
        required: true
        type: string
      install-flags:
        description: Extra flags for npm install
        required: false
        type: string
        default: ''

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install ${{ inputs.install-flags }}
        working-directory: ${{ inputs.package-dir }}

      - name: Run tests
        run: npm test
        working-directory: ${{ inputs.package-dir }}
